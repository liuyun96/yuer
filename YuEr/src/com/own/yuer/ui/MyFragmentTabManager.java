package com.own.yuer.ui;

import android.support.v4.app.FragmentActivity;
import android.widget.TabHost;

public class MyFragmentTabManager extends FragmentTabManager {

	public MyFragmentTabManager(FragmentActivity activity, TabHost tabHost,
			int containerId) {
		super(activity, tabHost, containerId);
	}

}
